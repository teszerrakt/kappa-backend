services:
  kappa-backend:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - kappa-data:/app/data
    restart: unless-stopped

volumes:
  kappa-data:
